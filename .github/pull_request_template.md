## Description
Briefly describe the change and the problem it solves.

## Checklist
- [ ] Minimal working diff (no unrequested refactoring / no boilerplate)
- [ ] `npm run typecheck` passes with zero errors (`tsc --noEmit`)
- [ ] `npm run build` succeeds cleanly
- [ ] `npm test` passes 100% of unit tests
- [ ] CJK full-width terminal display alignment preserved
- [ ] Zero repository pollution invariant preserved
